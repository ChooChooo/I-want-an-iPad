<%= form_tag(search_path, :method => :post, :id => 'search-form') do %>

    <input name="search_params" id="search_term" type="text" autofocus="autofocus" />
    <%= submit_tag "GO" %>

<% end %>

<%= render partial: "home/search_results" %>