<%= form_for(@project_type) do |f| %>
  <% if @project_type.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <strong><%= pluralize(@project_type.errors.count, 'error') %> prohibited this project_type from being saved:</strong>
      <% @project_type.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <span class="b_span">
    <%= f.submit %>
  </span>
<% end %>
