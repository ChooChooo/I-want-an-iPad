<table>
  <tbody>
    <% @skills.each do |skill| %>
      <tr>
        <td><%= skill.name %></td>
        <td><%= skill.description %></td>
        <td><input type="checkbox" id="post_validated" name="<%= skill.name %>" /></td>
      </tr>
    <% end %>
  </tbody>
</table>