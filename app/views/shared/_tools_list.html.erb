
<% unless @tools.blank? %>
    <% @tools.each do |tool| %>
      <div class="tool">
        <div class="tool_name"><%= tool.name %></div>
        <% state = "" %>
        <% unless @existing_tools.blank? %>
			<% if @existing_tools.include? tool.id %>
				<% state = "checked" %>
			<% end %>
   		<% end %>
        <div class="tool_check"><input type="checkbox" "<%=tool.name %>", id="<%=tool.id %>" name="<%=tool.id %>", <%=state %> /></div>
      </div>
    <% end %>
<% end %>